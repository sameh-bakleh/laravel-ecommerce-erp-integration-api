# Demo-only Docker environment (never use in production)
# Copy to .env.docker before: docker compose up --build
# See docs/docker.md for full setup and queue worker instructions.

APP_NAME="Laravel ERP Integration API"
APP_ENV=local
APP_KEY=base64:NkLXzqIhzNnGQOU5jdEPLBYZZIppbrNBkpOG4oRwjUM=
APP_DEBUG=true
APP_URL=http://localhost:8000

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=erp_integration
DB_USERNAME=root
DB_PASSWORD=

QUEUE_CONNECTION=database
CACHE_STORE=database
SESSION_DRIVER=array

# Sample integration credentials for local Docker demos only
INTEGRATION_API_TOKEN=docker-demo-token
ERP_WEBHOOK_SECRET=docker-demo-webhook-secret
ERP_SIMULATE_TRANSPORT_FAILURE=false
