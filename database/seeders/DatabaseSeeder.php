<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * This integration API is stateless for demo purposes — catalog and order
     * data are created through sync endpoints and the mock ERP client, not seeders.
     */
    public function run(): void
    {
        //
    }
}
